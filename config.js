// Site configuration — fill these in before launch.
window.MISTERFIT_CONFIG = {
  // Your Calendly (or other) booking link for qualified applicants.
  // Example: "https://calendly.com/your-handle/ugc-strategy-call"
  CALENDLY_URL: "",

  // Optional: a form endpoint (e.g. Formspree, Basin, Make webhook) that
  // receives every application as JSON. Leave empty to skip server capture —
  // qualified applicants still reach the booking screen either way.
  // Example: "https://formspree.io/f/xxxxxxxx"
  FORM_ENDPOINT: "",

  // Where "not right now" applicants are sent (low-ticket product or
  // community). Leave empty to show the waitlist message instead.
  DOWNSELL_URL: ""
};
